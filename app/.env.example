# Copy this file to .env.local (which is git-ignored) for local dev, and add the same
# variable in Cloudflare: your Worker project -> Settings -> Environment variables.
# Get a free key by email at https://web3forms.com — it's safe to be public, it only
# allows sending TO the owner's inbox, never reveals it.
VITE_FORM_KEY=
